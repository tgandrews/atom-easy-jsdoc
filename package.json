{
  "name": "atom-easy-jsdoc",
  "main": "./lib/index",
  "version": "4.4.0",
  "description": "JSDoc style comments for javascript development",
  "activationCommands": {
    "atom-workspace": [
      "jsdoc:block"
    ],
    "atom-text-editor": [
      "editor:newline"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tgandrews/atom-easy-jsdoc"
  },
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0"
  },
  "dependencies": {
    "acorn": "^3.0.4",
    "babel-preset-es2015": "^6.6.0"
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-eslint": "^6.0.0-beta.6",
    "chai": "^3.5.0",
    "eslint": "^2.4.0",
    "eslint-config-defaults": "^10.0.0-1",
    "mocha": "^2.4.5"
  },
  "scripts": {
    "test": "npm run lint && mocha --compilers js:babel-core/register tests/**/*.test.js",
    "lint": "eslint ."
  }
}
